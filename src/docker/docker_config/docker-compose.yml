version: '3'
services:
  web:
    build:
      context: ../../
      dockerfile: docker/docker_config/Dockerfile
    ports:
      - "3000:3000"
    depends_on:
      - redis
  redis:
    image: redis:alpine